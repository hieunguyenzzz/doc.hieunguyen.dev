---
name: Boilerplate
menu: Javascript
route: javascript/boilerplate
---

# Useful snippet

## log a object

```js
console.log(JSON.stringify(result, null, 4))
```

```
className={`${
            isExpanded ? `block` : `hidden`
          } md:block md:items-center w-full md:w-auto`}
```
